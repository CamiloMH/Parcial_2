const getAllTipoMedicamentos = (req,res) => {
    try {
        
    } catch (error) {
        res.status(400).json(error)
    }
}

const getTipoMedicamentoById = (req,res) => {
    try {
        
    } catch (error) {
        res.status(400).json(error)
    }
}

const createTipoMedicamento = (req,res) => {
    try {
        
    } catch (error) {
        res.status(400).json(error)
    }
}

const updateTipoMedicamento = (req,res) => {
    try {
        
    } catch (error) {
        res.status(400).json(error)
    }
}

const deleteTipoMedicamento = (req,res) => {
    try {
        
    } catch (error) {
        res.status(400).json(error)
    }
}

module.exports = {
    getAllTipoMedicamentos,
    getTipoMedicamentoById,
    createTipoMedicamento,
    updateTipoMedicamento,
    deleteTipoMedicamento
}